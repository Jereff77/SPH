{
  "project": {
    "name": "CRM Ventas SPH",
    "description": "Sistema de Gestión de Relaciones con Clientes para SPH Bines Raíces",
    "version": "1.0.0",
    "author": "SPH Bines Raíces",
    "repository": "https://github.com/Jereff77/SPH",
    "license": "MIT"
  },
  "easypanel": {
    "service_type": "App",
    "build_method": "Dockerfile",
    "dockerfile_path": "web/Dockerfile",
    "build_context": "web/",
    "internal_port": 8080,
    "health_check_path": "/api/health",
    "health_check_port": 8080,
    "note": "Dockerfile y build context son rutas relativas a la raíz del repositorio Git"
  },
  "build_args": {
    "description": "Configurar en la sección Build Args de Easypanel. Obligatorio para que Next.js hornee las variables en el bundle del browser.",
    "variables": [
      "NEXT_PUBLIC_SUPABASE_URL",
      "NEXT_PUBLIC_SUPABASE_ANON_KEY",
      "NEXT_PUBLIC_SITE_URL"
    ]
  },
  "environment_variables": {
    "description": "Configurar en la sección Environment Variables de Easypanel (runtime del contenedor).",
    "variables": [
      {
        "name": "NODE_ENV",
        "value": "production",
        "required": true
      },
      {
        "name": "NEXT_PUBLIC_SUPABASE_URL",
        "example": "https://szjlkvakwljssdnysazp.supabase.co",
        "required": true,
        "sensitive": true
      },
      {
        "name": "NEXT_PUBLIC_SUPABASE_ANON_KEY",
        "example": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
        "required": true,
        "sensitive": true
      },
      {
        "name": "NEXT_PUBLIC_SITE_URL",
        "example": "https://crm.sph-binesraices.com",
        "required": true
      }
    ]
  },
  "checklist": [
    "output: 'standalone' está en next.config.ts",
    "Dockerfile tiene 3 stages: deps, builder, runner",
    "Puerto 8080 y HOSTNAME=0.0.0.0 en el Dockerfile",
    "NEXT_PUBLIC_* configuradas como Build Args en Easypanel",
    "NEXT_PUBLIC_* también en Environment Variables en Easypanel",
    "Endpoint /api/health existe y devuelve 200",
    "Build Context en Easypanel apunta a web/ (donde está package.json)"
  ]
}
