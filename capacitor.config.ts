import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aceleremos.sphqr',
  appName: 'SPH Control de Accesos',
  webDir: 'dist'
};

export default config;
