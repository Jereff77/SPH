# Frontend — copiar a apps/web/.env. Solo variables con prefijo VITE_ se exponen al navegador.
# El frontend NUNCA recibe credenciales de Supabase: solo conoce la URL del backend.
VITE_API_URL=http://localhost:3001
